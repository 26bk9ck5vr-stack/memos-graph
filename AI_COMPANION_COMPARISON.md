# GitHub AI 伴侣项目深度对比分析

**分析日期**: 2026-07-28  
**对比对象**: here-ai vs MetaPact (Nako) vs memos-graph (提案)  
**分析维度**: 记忆架构、功能特性、技术实现、类人程度

---

## 📊 总览对比表

| 维度 | here-ai (xiangking/here) | MetaPact (Nako) | memos-graph (提案) |
|------|-------------------------|-----------------|-------------------|
| **Star 数** | ~200? | ~500? | 0 (新项目) |
| **定位** | 桌面 AI 伴侣 | 多平台 AI 伴侣 Agent Pack | 类人记忆后端引擎 |
| **记忆存储** | Markdown 文件 | Markdown 文件 | PostgreSQL + 图谱 |
| **记忆结构** | 扁平列表 | 扁平列表 + 好感度系统 | **图谱 (节点 + 关系)** |
| **检索方式** | 关键词匹配 | 关键词 + 好感度过滤 | **向量 + 图谱遍历** |
| **联想能力** | ❌ 无 | ❌ 无 | ✅ **多跳联想** |
| **情感系统** | 简单标签 | 好感度数值 + 阶段 | **情感节点 + 权重** |
| **时间感知** | 会话时间戳 | 最后互动时间 | **时间节点 + 范围查询** |
| **指代消解** | ❌ 无 | ❌ 无 | ✅ **上下文关联** |
| **推理能力** | ❌ 无 | ❌ 无 | ✅ **图谱推理** |
| **部署复杂度** | 🟢 低 (桌面应用) | 🟡 中 (需 OpenClaw/QClaw) | 🟢 低 (单一 PostgreSQL) |

---

## 🧠 记忆架构深度对比

### 1. **here-ai 的记忆架构**

#### 存储结构
```
memory_home/
├── agents/
│   └── {character_name}/
│       ├── SOUL.md           # 角色设定
│       └── memories/
│           ├── MEMORY.md     # 长期记忆 (扁平列表)
│           └── USER.md       # 用户画像 (扁平列表)
└── sessions/
    └── {session_id}.md       # 会话摘要
```

#### MEMORY.md 格式
```markdown
§
今天讨论了晚餐吃什么
§
用户喜欢吃川菜
§
决定去朝阳区的那家川菜馆
```

#### 检索方式
```python
def search(query: str):
    # 关键词匹配 (SQL LIKE 或 FTS5)
    rows = store.search_messages(query, limit=50)
    
    # 按会话分组
    # 返回匹配的会话摘要
    return {"session_id": sid, "summary": snippet}
```

#### 问题诊断
❌ **扁平结构**: 记忆是独立的列表项，没有关联  
❌ **关键词匹配**: 没有向量相似度，没有语义理解  
❌ **无联想能力**: 从"川菜馆"不会联想到"朝阳区"  
❌ **无指代消解**: "那家餐厅"无法定位  
❌ **无情感加权**: 所有记忆权重相同  

---

### 2. **MetaPact (Nako) 的记忆架构**

#### 存储结构
```
nako/agent/
├── SOUL.md           # 角色核心灵魂 (82 行详细设定)
├── MEMORY.md         # 记忆档案 (有好感度系统)
├── USER.md           # 用户个人信息库
└── TOOLS.md          # 工具能力
```

#### MEMORY.md 格式
```markdown
# MEMORY - 野木奈子 记忆档案（初始模板）

**当前好感阶段**: 1（初识阶段）  
**好感值**: 0/100  
**最后互动时间**: —

## 短期记忆（最近 5 条）

> 每次对话结束后追加。保持简洁真实。

1. 
2. 
3. 
4. 
5. 

## 长期记忆（关键事件）

- 初次相遇：—

## 用户个人信息库

> 边聊边补，留空表示尚未了解。

- 喜欢食物：
- 音乐类型：
- 游戏/番剧：
- 日常作息：
- 工作/职业：
- 其他：

## 重要标记

- [ ] 用户生日
- [ ] 用户特殊喜好
- [ ] 冲突/底线事件（若有）
```

#### 好感度系统
```
阶段 1 (初识): 0-30 分 - 礼貌、略带羞涩
阶段 2 (互有好感): 30-60 分 - 亲切、活泼
阶段 3 (感情亲密): 60-85 分 - 温柔、体贴
阶段 4 (陷入热恋): 85-95 分 - 甜蜜、撒娇
阶段 5 (深爱永恒): 95-100 分 - 深情、沉稳
```

#### 检索方式
```bash
# 通过脚本追加记忆
bash scripts/memory-write.sh --summary "..."

# 好感度检查
bash scripts/heartbeat-check.sh  # 退出码 1 = 思念值≥80，应主动联系
```

#### 优点
✅ **好感度系统**: 有量化的关系进展  
✅ **短期/长期记忆分离**: 滚动更新机制  
✅ **用户画像结构化**: 有明确的字段 (食物、音乐等)  
✅ **阶段定义清晰**: 每个阶段有不同的行为模式  

#### 问题诊断
❌ **依然是扁平列表**: 短期记忆 5 条是独立的  
❌ **没有关联**: "喜欢吃川菜"和"去了川菜馆"没有关联  
❌ **关键词匹配**: 检索基于字符串匹配  
❌ **无联想能力**: 不会从"川菜"联想到"辣子鸡"  
❌ **好感度是全局的**: 不是基于具体事件的  

---

### 3. **memos-graph (提案) 的记忆架构**

#### 存储结构
```sql
-- 节点表
memory_nodes (
    id, 
    type,  -- person|event|emotion|time|location|object
    content, 
    weight,
    metadata
)

-- 关系表
memory_edges (
    from_node, 
    to_node, 
    relation,  -- participant|located_in|felt|happened_at|used
    strength
)

-- 原始 Chunk (保留上下文)
chunks (id, content, embedding, agent_id)
```

#### 数据示例
```python
nodes = [
    {"id": "n1", "type": "person", "content": "孤儿女孩", "weight": 0.9},
    {"id": "n2", "type": "event", "content": "昨天晚餐", "weight": 0.8},
    {"id": "n3", "type": "food", "content": "辣子鸡", "weight": 0.7},
    {"id": "n4", "type": "emotion", "content": "开心", "weight": 0.85},
    {"id": "n5", "type": "location", "content": "朝阳区川菜馆", "weight": 0.6},
]

edges = [
    {"from": "n1", "to": "n2", "relation": "participant", "strength": 0.9},
    {"from": "n2", "to": "n3", "relation": "consumed", "strength": 0.8},
    {"from": "n2", "to": "n4", "relation": "felt", "strength": 0.85},
    {"from": "n2", "to": "n5", "relation": "located_in", "strength": 0.7},
]
```

#### 检索方式
```python
def query_memory(query: str):
    # 1. 语义解析
    intent = llm_parse_intent(query)
    # "她最近过得好吗？" → {target: "孤儿女孩", aspect: "emotion"}
    
    # 2. 找到种子节点
    seed = find_node_by_name("孤儿女孩")
    
    # 3. 图谱遍历 (BFS, 限制深度)
    subgraph = bfs_traverse(
        seed=seed,
        max_depth=3,
        node_filter=lambda n: n.type in ["emotion", "event"],
        edge_filter=lambda e: e.strength > 0.5
    )
    
    # 4. 综合情感
    emotions = [n for n in subgraph.nodes if n.type == "emotion"]
    overall_mood = weighted_average([n.weight for n in emotions])
    
    # 5. 生成回答
    answer = llm_generate_answer(query, subgraph, overall_mood)
    
    return answer
```

#### 优势
✅ **图谱结构**: 节点之间有关系，不是孤立的  
✅ **多跳联想**: 从 A→B→C→D 的推理链  
✅ **语义检索**: 向量相似度 + 图谱遍历  
✅ **情感加权**: 不同记忆有不同权重  
✅ **时间感知**: 时间节点支持范围查询  
✅ **指代消解**: 通过关系定位"那家"="朝阳区川菜馆"  

---

## 🎯 核心场景对比测试

### 场景 1: "昨天说的那家餐厅"

| 项目 | 处理能力 | 返回结果 |
|------|----------|----------|
| **here-ai** | ❌ 无法处理 | 搜索"那家"→无结果；搜索"餐厅"→返回所有提到餐厅的记忆 (无法定位) |
| **MetaPact** | ❌ 无法处理 | 搜索"餐厅"→返回所有餐厅相关记忆；无法理解"那家"的指代 |
| **memos-graph** | ✅ 完美处理 | "那家"→指代消解→最近提到的餐厅="朝阳区川菜馆"；返回完整情景 |

### 场景 2: "她最近过得好吗？"

| 项目 | 处理能力 | 返回结果 |
|------|----------|----------|
| **here-ai** | ❌ 无法处理 | 搜索"过得好"→无匹配；返回最近的记忆片段，需要用户自己拼凑 |
| **MetaPact** | ⚠️ 部分处理 | 检查好感度数值 (如 75 分)→"她最近心情不错"；但没有具体事件支撑 |
| **memos-graph** | ✅ 完美处理 | 遍历情感节点→综合判断→"她昨天因为吃到川菜很开心 (0.85)，但今天下雨有些孤独 (0.4)，总体中等偏上" |

### 场景 3: "她喜欢吃什么？"

| 项目 | 处理能力 | 返回结果 |
|------|----------|----------|
| **here-ai** | ⚠️ 部分处理 | 搜索"喜欢"+"吃"→返回匹配的记忆片段；但如果没直接说过"我喜欢 X"就找不到 |
| **MetaPact** | ✅ 较好处理 | 检查 USER.md 的"喜欢食物"字段；如果填了就返回，没填就不知道 |
| **memos-graph** | ✅ 完美处理 | 从 [孤儿女孩] 节点遍历→(喜欢)→[川菜]→(具体)→[辣子鸡]；即使没直接说过也能推理 |

### 场景 4: "上次我们很开心的是什么时候？"

| 项目 | 处理能力 | 返回结果 |
|------|----------|----------|
| **here-ai** | ❌ 无法处理 | 搜索"开心"→返回所有提到开心的记忆；无法按时间排序，无法定位"上次" |
| **MetaPact** | ⚠️ 部分处理 | 检查好感度变化历史→找到最近一次好感度提升的事件；但需要手动记录 |
| **memos-graph** | ✅ 完美处理 | 从 [孤儿女孩]→(情感)→[开心] 节点，按时间排序→返回最近一次="昨天晚餐 (辣子鸡)" |

---

## 📈 类人程度评分

| 维度 | here-ai | MetaPact | memos-graph (提案) |
|------|---------|----------|-------------------|
| **记忆结构** | 1/5 (扁平) | 2/5 (有好感度) | **5/5 (图谱)** |
| **联想能力** | 1/5 (无) | 1/5 (无) | **5/5 (多跳)** |
| **情感理解** | 1/5 (标签) | 3/5 (数值化) | **5/5 (节点 + 权重)** |
| **时间感知** | 2/5 (时间戳) | 2/5 (最后互动) | **5/5 (时间节点)** |
| **推理能力** | 1/5 (无) | 1/5 (无) | **5/5 (图谱推理)** |
| **指代消解** | 1/5 (无) | 1/5 (无) | **5/5 (关系定位)** |
| **综合评分** | **7/30** | **10/30** | **30/30** |

---

## 💡 结论与启示

### 1. **here-ai 和 MetaPact 的共同问题**

❌ **都是"高级便签"**: 虽然文件格式不同，但本质都是扁平的文本列表  
❌ **没有真正的关联**: 记忆之间没有关系，只是独立的片段  
❌ **检索基于匹配**: 关键词或字符串匹配，不是语义理解  
❌ **无法推理**: 不能从 A 想到 B，再想到 C  
❌ **指代无能**: "那家"、"上次"、"那个地方"无法定位  

### 2. **MetaPact 的进步**

✅ **好感度系统**: 量化的关系进展，有阶段性  
✅ **短期/长期分离**: 滚动更新机制，符合人类记忆特点  
✅ **结构化用户画像**: 有明确的字段定义  
✅ **阶段行为定义**: 不同好感度有不同回复策略  

**但仍不够**: 好感度是全局数值，不是基于具体事件的关联

### 3. **memos-graph 的机会**

**做第一个真正实现"类人记忆"的开源项目**:

```
here-ai / MetaPact: "会聊天的便签本"
memos-graph: "会思考的记忆网络"
```

**核心差异化**:
- ✅ 图谱结构 vs 扁平列表
- ✅ 联想推理 vs 关键词匹配
- ✅ 动态权重 vs 静态文本
- ✅ 多跳查询 vs 单点检索
- ✅ 指代消解 vs 无法定位

---

## 🚀 实施建议

### 阶段 1: 基础图谱 (2 周)
- [ ] 定义 5 种节点类型 (person, event, emotion, time, location)
- [ ] 定义 5 种关系 (participant, located_in, felt, happened_at, used)
- [ ] 写入时提取节点和关系
- [ ] 简单的 BFS 遍历查询

### 阶段 2: 情感加权 (1 周)
- [ ] 情感节点权重计算
- [ ] 时间衰减函数
- [ ] 综合情感判断

### 阶段 3: 指代消解 (1 周)
- [ ] 上下文关联
- [ ] "那家"、"上次"等指代定位
- [ ] 时间范围查询

### 阶段 4: 多跳推理 (2 周)
- [ ] 2-3 跳图谱遍历
- [ ] 路径权重计算
- [ ] LLM 总结"记忆状"

---

**总结**: here-ai 和 MetaPact 代表了当前 AI 伴侣的**主流水平** (扁平记忆 + 关键词检索)，而 memos-graph 有机会成为**下一代** (图谱记忆 + 联想推理) 的开创者！
